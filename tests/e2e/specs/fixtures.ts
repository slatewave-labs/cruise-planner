/**
 * Shared fixtures and helpers for E2E tests.
 *
 * For the template app, no API mocking is needed for basic navigation tests.
 * Extend this file as your app grows to include API route mocking or test data factories.
 */

// Test data constants (add as needed for your app)
export const TEST_ITEM_NAME = 'Test Item';
