name: Deploy to Production

on:
  # Deploy when a version tag is pushed (e.g., v1.0.0)
  push:
    tags:
      - "v*"

  # Allow manual deployment
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (leave empty for latest)"
        required: false
        type: string

# Cancel in-progress runs for the same environment/branch
concurrency:
  group: deploy-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ secrets.AWS_REGION || 'us-east-1' }}
  ENVIRONMENT: prod
  PROJECT_NAME: shoreexplorer
  BACKEND_ECR_REPO: shoreexplorer-backend
  FRONTEND_ECR_REPO: shoreexplorer-frontend

jobs:
  # ─────────────────────────────────────────────────
  # Run CI first to make sure everything passes
  # ─────────────────────────────────────────────────
  ci-check:
    name: CI Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Start DynamoDB Local
        run: |
          docker run -d -p 8000:8000 amazon/dynamodb-local:latest \
            -jar DynamoDBLocal.jar -sharedDb -inMemory
          echo "DynamoDB Local container started"
          sleep 5  # Give DynamoDB Local time to initialize

      - name: Wait for DynamoDB Local to be ready
        run: |
          echo "Waiting for DynamoDB Local to start..."
          pip install boto3  # Install AWS SDK for testing
          for i in {1..30}; do
            if python3 -c "import boto3; dynamodb = boto3.client('dynamodb', endpoint_url='http://localhost:8000', region_name='us-east-1', aws_access_key_id='dummy', aws_secret_access_key='dummy'); dynamodb.list_tables()" 2>/dev/null; then
              echo "DynamoDB Local is ready!"
              exit 0
            fi
            echo "Attempt $i: DynamoDB Local not ready yet, waiting..."
            sleep 2
          done
          echo "DynamoDB Local failed to start"
          docker ps -a
          docker logs $(docker ps -aq --filter ancestor=amazon/dynamodb-local:latest)
          exit 1

      - name: Install backend dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install pytest pytest-asyncio httpx black flake8 isort

      - name: Create DynamoDB test table
        run: |
          python3 << 'EOF'
          import boto3
          
          # Create DynamoDB client
          dynamodb = boto3.client(
              'dynamodb',
              endpoint_url='http://localhost:8000',
              region_name='us-east-1',
              aws_access_key_id='dummy',
              aws_secret_access_key='dummy'
          )
          
          # Create table
          try:
              dynamodb.create_table(
                  TableName='shoreexplorer_test',
                  KeySchema=[
                      {'AttributeName': 'PK', 'KeyType': 'HASH'},
                      {'AttributeName': 'SK', 'KeyType': 'RANGE'}
                  ],
                  AttributeDefinitions=[
                      {'AttributeName': 'PK', 'AttributeType': 'S'},
                      {'AttributeName': 'SK', 'AttributeType': 'S'},
                      {'AttributeName': 'GSI1PK', 'AttributeType': 'S'},
                      {'AttributeName': 'GSI1SK', 'AttributeType': 'S'}
                  ],
                  GlobalSecondaryIndexes=[
                      {
                          'IndexName': 'GSI1',
                          'KeySchema': [
                              {'AttributeName': 'GSI1PK', 'KeyType': 'HASH'},
                              {'AttributeName': 'GSI1SK', 'KeyType': 'RANGE'}
                          ],
                          'Projection': {'ProjectionType': 'ALL'},
                          'ProvisionedThroughput': {
                              'ReadCapacityUnits': 5,
                              'WriteCapacityUnits': 5
                          }
                      }
                  ],
                  BillingMode='PROVISIONED',
                  ProvisionedThroughput={
                      'ReadCapacityUnits': 5,
                      'WriteCapacityUnits': 5
                  }
              )
              print("Table 'shoreexplorer_test' created successfully!")
          except Exception as e:
              print(f"Error creating table: {e}")
              raise
          EOF

      - name: Backend lint
        run: cd backend && black --check . && isort --check-only . && flake8 .

      - name: Backend tests
        run: |
          cd backend
          pytest tests/ -v --tb=short
        env:
          DYNAMODB_TABLE_NAME: shoreexplorer_test
          AWS_DEFAULT_REGION: us-east-1
          DYNAMODB_ENDPOINT_URL: http://localhost:8000
          AWS_ACCESS_KEY_ID: dummy
          AWS_SECRET_ACCESS_KEY: dummy
          GROQ_API_KEY: test-key-not-real

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: yarn
          cache-dependency-path: frontend/yarn.lock

      - name: Frontend tests
        run: |
          cd frontend
          yarn install --frozen-lockfile
          yarn test --watchAll=false --ci
        env:
          CI: true

  # ─────────────────────────────────────────────────
  # Check if production environment is deployed
  # ─────────────────────────────────────────────────
  check-env:
    name: Check Production Environment is Deployed
    runs-on: ubuntu-latest
    environment: production
    outputs:
      deployed: ${{ steps.check.outputs.deployed }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Check if production environment is deployed
        id: check
        run: |
          CLUSTER_STATUS=$(aws ecs describe-clusters \
            --clusters "${PROJECT_NAME}-${ENVIRONMENT}-cluster" \
            --query "clusters[0].status || 'NOT_FOUND'" \
            --output text --region "$AWS_REGION" 2>/dev/null || echo "NOT_FOUND")

          if [[ "$CLUSTER_STATUS" == "ACTIVE" ]]; then
            echo "✅ Production environment is deployed (cluster is ACTIVE)"
            echo "deployed=true" >> "$GITHUB_OUTPUT"
          else
            echo "⏭️  Production environment is not deployed (cluster status: ${CLUSTER_STATUS}). Skipping deployment."
            echo "deployed=false" >> "$GITHUB_OUTPUT"
          fi

  # ─────────────────────────────────────────────────
  # Deploy to production (requires approval)
  # ─────────────────────────────────────────────────
  deploy-prod:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [ci-check, check-env]
    environment: production  # Requires manual approval if configured

    if: needs.check-env.outputs.deployed == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image variables
        id: vars
        env:
          GITHUB_SHA: ${{ github.sha }}
          INPUT_IMAGE_TAG: ${{ inputs.image_tag }}
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_BASE="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          GIT_SHA=$(echo "$GITHUB_SHA" | cut -c1-7)

          # Use provided tag or generate one
          if [[ -n "$INPUT_IMAGE_TAG" ]]; then
            IMAGE_TAG="$INPUT_IMAGE_TAG"
          else
            IMAGE_TAG="${ENVIRONMENT}-${GIT_SHA}"
          fi

          echo "account_id=${ACCOUNT_ID}" >> "$GITHUB_OUTPUT"
          echo "ecr_base=${ECR_BASE}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          echo "backend_image=${ECR_BASE}/${BACKEND_ECR_REPO}" >> "$GITHUB_OUTPUT"
          echo "frontend_image=${ECR_BASE}/${FRONTEND_ECR_REPO}" >> "$GITHUB_OUTPUT"

      - name: Get ALB DNS
        id: alb
        env:
          CUSTOM_DOMAIN: ${{ secrets.PROD_DOMAIN || '' }}
        run: |
          ALB_DNS=$(aws elbv2 describe-load-balancers \
            --names "${PROJECT_NAME}-${ENVIRONMENT}-alb" \
            --query "LoadBalancers[0].DNSName" \
            --output text --region "$AWS_REGION" 2>/dev/null || echo "")

          # Use custom domain if configured, otherwise use ALB DNS
          if [[ -n "$CUSTOM_DOMAIN" ]]; then
            # Production environment uses apex domain (no subdomain)
            BACKEND_URL="https://${CUSTOM_DOMAIN}"
            echo "Using custom domain: ${CUSTOM_DOMAIN}"
          elif [[ -n "$ALB_DNS" && "$ALB_DNS" != "None" ]]; then
            BACKEND_URL="https://${ALB_DNS}"
            echo "Using ALB DNS: ${ALB_DNS}"
          else
            BACKEND_URL="http://localhost:8001"
            echo "Using localhost fallback"
          fi

          echo "dns=${ALB_DNS}" >> "$GITHUB_OUTPUT"
          echo "backend_url=${BACKEND_URL}" >> "$GITHUB_OUTPUT"

      # ── Build and push images (optimized with caching) ──────────
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend image
        id: build-backend
        uses: docker/build-push-action@v5
        with:
          context: backend
          push: true
          tags: |
            ${{ steps.vars.outputs.backend_image }}:${{ steps.vars.outputs.image_tag }}
            ${{ steps.vars.outputs.backend_image }}:${{ env.ENVIRONMENT }}-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend image
        id: build-frontend
        uses: docker/build-push-action@v5
        with:
          context: frontend
          push: true
          build-args: |
            REACT_APP_BACKEND_URL=${{ steps.alb.outputs.backend_url }}
          tags: |
            ${{ steps.vars.outputs.frontend_image }}:${{ steps.vars.outputs.image_tag }}
            ${{ steps.vars.outputs.frontend_image }}:${{ env.ENVIRONMENT }}-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── Deploy to ECS (rolling deployment) ─────────────────────
      # First, register updated task definitions with correct env vars
      - name: Get secrets ARN
        id: secrets
        run: |
          SECRET_ARN=$(aws secretsmanager describe-secret \
            --secret-id "${PROJECT_NAME}-${ENVIRONMENT}-secrets" \
            --query 'ARN' --output text --region "$AWS_REGION" 2>/dev/null || echo "")
          
          if [[ -z "$SECRET_ARN" ]]; then
            echo "Warning: Could not find secrets ARN, skipping task definition update"
            echo "secret_arn=" >> "$GITHUB_OUTPUT"
          else
            echo "secret_arn=${SECRET_ARN}" >> "$GITHUB_OUTPUT"
          fi

      - name: Register backend task definition
        if: steps.secrets.outputs.secret_arn != ''
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_BASE="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          SECRET_ARN="${{ steps.secrets.outputs.secret_arn }}"
          
          # Get IAM role ARNs
          EXECUTION_ROLE_ARN=$(aws iam get-role \
            --role-name "${PROJECT_NAME}-${ENVIRONMENT}-ecs-task-execution-role" \
            --query 'Role.Arn' --output text --region "$AWS_REGION")
          TASK_ROLE_ARN=$(aws iam get-role \
            --role-name "${PROJECT_NAME}-${ENVIRONMENT}-ecs-task-role" \
            --query 'Role.Arn' --output text --region "$AWS_REGION")
          
          # Register backend task definition with GROQ_API_KEY
          aws ecs register-task-definition \
            --family "${PROJECT_NAME}-${ENVIRONMENT}-backend-task" \
            --network-mode awsvpc \
            --requires-compatibilities FARGATE \
            --cpu 256 \
            --memory 512 \
            --execution-role-arn "$EXECUTION_ROLE_ARN" \
            --task-role-arn "$TASK_ROLE_ARN" \
            --container-definitions "[{
              \"name\": \"backend\",
              \"image\": \"${ECR_BASE}/${BACKEND_ECR_REPO}:${ENVIRONMENT}-latest\",
              \"essential\": true,
              \"portMappings\": [{\"containerPort\": 8001, \"protocol\": \"tcp\"}],
              \"secrets\": [
                {\"name\": \"GROQ_API_KEY\", \"valueFrom\": \"${SECRET_ARN}:GROQ_API_KEY::\"}
              ],
              \"environment\": [
                {\"name\": \"DYNAMODB_TABLE_NAME\", \"value\": \"${PROJECT_NAME}-${ENVIRONMENT}\"},
                {\"name\": \"AWS_DEFAULT_REGION\", \"value\": \"${AWS_REGION}\"},
                {\"name\": \"ENVIRONMENT\", \"value\": \"${ENVIRONMENT}\"},
                {\"name\": \"ENABLE_CLOUDWATCH_METRICS\", \"value\": \"true\"}
              ],
              \"logConfiguration\": {
                \"logDriver\": \"awslogs\",
                \"options\": {
                  \"awslogs-group\": \"/ecs/${PROJECT_NAME}-${ENVIRONMENT}-backend\",
                  \"awslogs-region\": \"${AWS_REGION}\",
                  \"awslogs-stream-prefix\": \"ecs\"
                }
              },
              \"healthCheck\": {
                \"command\": [\"CMD-SHELL\", \"python -c \\\"import urllib.request; exit(0 if urllib.request.urlopen('http://localhost:8001/api/health').status == 200 else 1)\\\"\"],
                \"interval\": 30,
                \"timeout\": 10,
                \"retries\": 3,
                \"startPeriod\": 60
              }
            }]" \
            --tags "key=Project,value=${PROJECT_NAME}" "key=Environment,value=${ENVIRONMENT}" \
            --region "$AWS_REGION"

      - name: Register frontend task definition
        if: steps.secrets.outputs.secret_arn != ''
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_BASE="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          
          # Get IAM role ARNs
          EXECUTION_ROLE_ARN=$(aws iam get-role \
            --role-name "${PROJECT_NAME}-${ENVIRONMENT}-ecs-task-execution-role" \
            --query 'Role.Arn' --output text --region "$AWS_REGION")
          TASK_ROLE_ARN=$(aws iam get-role \
            --role-name "${PROJECT_NAME}-${ENVIRONMENT}-ecs-task-role" \
            --query 'Role.Arn' --output text --region "$AWS_REGION")
          
          # Register frontend task definition
          aws ecs register-task-definition \
            --family "${PROJECT_NAME}-${ENVIRONMENT}-frontend-task" \
            --network-mode awsvpc \
            --requires-compatibilities FARGATE \
            --cpu 256 \
            --memory 512 \
            --execution-role-arn "$EXECUTION_ROLE_ARN" \
            --task-role-arn "$TASK_ROLE_ARN" \
            --container-definitions "[{
              \"name\": \"frontend\",
              \"image\": \"${ECR_BASE}/${FRONTEND_ECR_REPO}:${ENVIRONMENT}-latest\",
              \"essential\": true,
              \"portMappings\": [{\"containerPort\": 8080, \"protocol\": \"tcp\"}],
              \"logConfiguration\": {
                \"logDriver\": \"awslogs\",
                \"options\": {
                  \"awslogs-group\": \"/ecs/${PROJECT_NAME}-${ENVIRONMENT}-frontend\",
                  \"awslogs-region\": \"${AWS_REGION}\",
                  \"awslogs-stream-prefix\": \"ecs\"
                }
              },
              \"healthCheck\": {
                \"command\": [\"CMD-SHELL\", \"wget --quiet --tries=1 --spider http://localhost:8080/ || exit 1\"],
                \"interval\": 30,
                \"timeout\": 5,
                \"retries\": 3,
                \"startPeriod\": 30
              }
            }]" \
            --tags "key=Project,value=${PROJECT_NAME}" "key=Environment,value=${ENVIRONMENT}" \
            --region "$AWS_REGION"

      # ── Deploy via CodeDeploy Blue/Green ─────────────────────────
      - name: Deploy backend via CodeDeploy
        id: deploy-backend
        run: |
          APP_NAME="${PROJECT_NAME}-${ENVIRONMENT}"
          CODEDEPLOY_APP="${APP_NAME}-codedeploy"

          # Get the latest backend task definition ARN
          TASK_DEF_ARN=$(aws ecs describe-task-definition \
            --task-definition "${APP_NAME}-backend-task" \
            --query 'taskDefinition.taskDefinitionArn' --output text \
            --region "$AWS_REGION")

          # Create AppSpec inline for backend
          APPSPEC=$(cat <<EOF
          {
            "version": 0.0,
            "Resources": [{
              "TargetService": {
                "Type": "AWS::ECS::Service",
                "Properties": {
                  "TaskDefinition": "${TASK_DEF_ARN}",
                  "LoadBalancerInfo": {
                    "ContainerName": "backend",
                    "ContainerPort": 8001
                  }
                }
              }
            }]
          }
          EOF
          )

          DEPLOY_ID=$(aws deploy create-deployment \
            --application-name "$CODEDEPLOY_APP" \
            --deployment-group-name "${APP_NAME}-backend-dg" \
            --revision "revisionType=AppSpecContent,appSpecContent={content='$(echo "$APPSPEC" | jq -c .)'}" \
            --description "Backend deploy: ${{ steps.vars.outputs.image_tag }}" \
            --query 'deploymentId' --output text \
            --region "$AWS_REGION")
          echo "🚀 Backend CodeDeploy deployment started: ${DEPLOY_ID}"
          echo "deployment_id=${DEPLOY_ID}" >> "$GITHUB_OUTPUT"

      - name: Deploy frontend via CodeDeploy
        id: deploy-frontend
        run: |
          APP_NAME="${PROJECT_NAME}-${ENVIRONMENT}"
          CODEDEPLOY_APP="${APP_NAME}-codedeploy"

          # Get the latest frontend task definition ARN
          TASK_DEF_ARN=$(aws ecs describe-task-definition \
            --task-definition "${APP_NAME}-frontend-task" \
            --query 'taskDefinition.taskDefinitionArn' --output text \
            --region "$AWS_REGION")

          # Create AppSpec inline for frontend
          APPSPEC=$(cat <<EOF
          {
            "version": 0.0,
            "Resources": [{
              "TargetService": {
                "Type": "AWS::ECS::Service",
                "Properties": {
                  "TaskDefinition": "${TASK_DEF_ARN}",
                  "LoadBalancerInfo": {
                    "ContainerName": "frontend",
                    "ContainerPort": 8080
                  }
                }
              }
            }]
          }
          EOF
          )

          DEPLOY_ID=$(aws deploy create-deployment \
            --application-name "$CODEDEPLOY_APP" \
            --deployment-group-name "${APP_NAME}-frontend-dg" \
            --revision "revisionType=AppSpecContent,appSpecContent={content='$(echo "$APPSPEC" | jq -c .)'}" \
            --description "Frontend deploy: ${{ steps.vars.outputs.image_tag }}" \
            --query 'deploymentId' --output text \
            --region "$AWS_REGION")
          echo "🚀 Frontend CodeDeploy deployment started: ${DEPLOY_ID}"
          echo "deployment_id=${DEPLOY_ID}" >> "$GITHUB_OUTPUT"

      # ── Async Deployment Callback ────────────────────────────────
      # EventBridge rules (created by setup workflow) listen for CodeDeploy
      # deployment state changes and dispatch to deploy-callback.yml.
      # The callback approves the production-post-deploy environment gate once
      # BOTH backend and frontend deployments succeed.

      # ── Monitoring & Alerting ───────────────────────────────────
      - name: Setup monitoring (dashboard + alarms)
        env:
          ALERT_EMAIL: ${{ secrets.ALERT_EMAIL || '' }}
        run: |
          chmod +x infra/aws/scripts/10-setup-monitoring.sh
          bash infra/aws/scripts/10-setup-monitoring.sh "${ENVIRONMENT}"
        continue-on-error: true  # Monitoring setup should not block deployment

      - name: Deployment summary
        if: always()
        env:
          BACKEND_URL: ${{ steps.alb.outputs.backend_url }}
        run: |
          echo "## Production Deployment Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Environment | \`production\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Image Tag | \`${{ steps.vars.outputs.image_tag }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Commit | \`${{ github.sha }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag | \`${{ github.ref_name }}\` |" >> "$GITHUB_STEP_SUMMARY"
          if [[ "${BACKEND_URL}" != "http://localhost:8001" ]]; then
            echo "| App URL | ${BACKEND_URL} |" >> "$GITHUB_STEP_SUMMARY"
            echo "| API Health | ${BACKEND_URL}/api/health |" >> "$GITHUB_STEP_SUMMARY"
            DASHBOARD_URL="https://${AWS_REGION}.console.aws.amazon.com/cloudwatch/home?region=${AWS_REGION}#dashboards:name=${PROJECT_NAME}-${ENVIRONMENT}-dashboard"
            echo "| Dashboard | [CloudWatch](${DASHBOARD_URL}) |" >> "$GITHUB_STEP_SUMMARY"
          fi

  # ─────────────────────────────────────────────────
  # Rollback job (manual trigger only)
  # ─────────────────────────────────────────────────
  rollback:
    name: Rollback Production
    runs-on: ubuntu-latest
    if: failure() && needs.deploy-prod.result == 'failure'
    needs: deploy-prod
    environment: production

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Rollback via CodeDeploy
        run: |
          echo "⚠️  Deployment failed. Rolling back via CodeDeploy..."
          APP_NAME="${PROJECT_NAME}-${ENVIRONMENT}"
          CODEDEPLOY_APP="${APP_NAME}-codedeploy"

          for DG in "${APP_NAME}-backend-dg" "${APP_NAME}-frontend-dg"; do
            # Get the most recent deployment for this deployment group
            DEPLOY_ID=$(aws deploy list-deployments \
              --application-name "$CODEDEPLOY_APP" \
              --deployment-group-name "$DG" \
              --query "deployments[0]" --output text \
              --region "$AWS_REGION" 2>/dev/null || echo "")

            if [[ -n "$DEPLOY_ID" && "$DEPLOY_ID" != "None" ]]; then
              STATUS=$(aws deploy get-deployment --deployment-id "$DEPLOY_ID" \
                --query "deploymentInfo.status" --output text \
                --region "$AWS_REGION" 2>/dev/null || echo "")

              if [[ "$STATUS" == "Succeeded" ]]; then
                # CodeDeploy auto-rollback creates a new deployment with the previous revision
                aws deploy stop-deployment --deployment-id "$DEPLOY_ID" \
                  --auto-rollback-enabled \
                  --region "$AWS_REGION" 2>/dev/null || true
                echo "  ⚠️  Stopped deployment ${DEPLOY_ID} for ${DG} (auto-rollback triggered)"
              elif [[ "$STATUS" == "InProgress" || "$STATUS" == "Created" || "$STATUS" == "Queued" ]]; then
                aws deploy stop-deployment --deployment-id "$DEPLOY_ID" \
                  --auto-rollback-enabled \
                  --region "$AWS_REGION" 2>/dev/null || true
                echo "  ⚠️  Stopped in-progress deployment ${DEPLOY_ID} for ${DG}"
              else
                echo "  ℹ️  Deployment ${DEPLOY_ID} for ${DG} is in state: ${STATUS}"
              fi
            else
              echo "  ℹ️  No deployments found for ${DG}"
            fi
          done
          echo "Rollback initiated. CodeDeploy will handle the blue/green swap back."

  # ─────────────────────────────────────────────────
  # Post-deployment cleanup and smoke tests
  # Runs ONCE after both services have deployed
  # ─────────────────────────────────────────────────
  cleanup-and-smoke:
    name: Deployment Cleanup & Smoke Test
    runs-on: ubuntu-latest
    needs: deploy-prod
    if: always() && needs.deploy-prod.result != 'skipped'
    environment: production-post-deploy

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get deployment URL
        id: alb
        env:
          CUSTOM_DOMAIN: ${{ secrets.PROD_DOMAIN || '' }}
        run: |
          ALB_DNS=$(aws elbv2 describe-load-balancers \
            --names "${PROJECT_NAME}-${ENVIRONMENT}-alb" \
            --query "LoadBalancers[0].DNSName" \
            --output text --region "$AWS_REGION" 2>/dev/null || echo "")

          if [[ -n "$CUSTOM_DOMAIN" ]]; then
            URL="https://${CUSTOM_DOMAIN}"
          elif [[ -n "$ALB_DNS" && "$ALB_DNS" != "None" ]]; then
            URL="https://${ALB_DNS}"
          else
            URL="http://localhost:8001"
          fi
          echo "url=${URL}" >> "$GITHUB_OUTPUT"

      # NOTE: No ECS polling needed — the environment gate (production-post-deploy)
      # holds this job until the CodeDeploy callback approves it, meaning
      # services are already stable when this job starts running.

      - name: Run smoke tests
        if: needs.deploy-prod.result == 'success' && steps.alb.outputs.url != 'http://localhost:8001'
        run: |
          URL="${{ steps.alb.outputs.url }}"
          echo "Running smoke tests against ${URL}"
          sleep 15

          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            "${URL}/api/health" --max-time 30 --retry 3 --retry-delay 10 || echo "000")
          if [[ "$HTTP_CODE" == "200" ]]; then
            echo "✅ Health check passed (HTTP $HTTP_CODE)"
          else
            echo "❌ Health check failed (HTTP $HTTP_CODE)"
            exit 1
          fi

          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            "${URL}/" --max-time 30 --retry 3 --retry-delay 10 || echo "000")
          if [[ "$HTTP_CODE" == "200" ]]; then
            echo "✅ Frontend accessible (HTTP $HTTP_CODE)"
          else
            echo "❌ Frontend check failed (HTTP $HTTP_CODE)"
            exit 1
          fi

      - name: Smoke test & cleanup summary
        if: always()
        run: |
          DEPLOY_STATUS="${{ needs.deploy-prod.result }}"
          echo "## Deployment Cleanup & Smoke Test" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Item | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Environment | \`${ENVIRONMENT}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Deploy Result | \`${DEPLOY_STATUS}\` |" >> "$GITHUB_STEP_SUMMARY"
          if [[ "${{ steps.alb.outputs.url }}" != "" && "${{ steps.alb.outputs.url }}" != "http://localhost:8001" ]]; then
            echo "| App URL | ${{ steps.alb.outputs.url }} |" >> "$GITHUB_STEP_SUMMARY"
            echo "| API Health | ${{ steps.alb.outputs.url }}/api/health |" >> "$GITHUB_STEP_SUMMARY"
          fi
